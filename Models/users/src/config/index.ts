export const HOST: string = process.env.HOST || 'localhost';

export const PORT: number = parseInt(process.env.PORT) || 13306;

export const USERNAME: string = process.env.USERNAME || 'atlas';

export const PASSWORD: string = process.env.PASSWORD || 'Atlas1234';

export const DATABASE: string = process.env.DATABASE || 'users';
